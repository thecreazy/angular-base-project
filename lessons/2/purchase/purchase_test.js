'use strict';

describe('myApp.purchase module', function() {

  beforeEach(module('myApp.purchase'));

  describe('purchase controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var purchaseCtrl = $controller('PurchaseCtrl');
      expect(purchaseCtrl).toBeDefined();
    }));

  });
});